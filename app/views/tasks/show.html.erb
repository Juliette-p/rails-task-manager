<h1><%=@task.title%></h1>
<p><%=@task.details%></p>

<%# <%= if @task.completed == "true" ? <p>"Task is completed"</p> : <p>"Task is not completed yet"</p> %>
<%=link_to "Modify", edit_task_path %>
<%=link_to "Go back to the list", index_path%>
<%=link_to "Delete", task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
